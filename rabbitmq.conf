# RabbitMQ 4.x Configuration
# This file addresses deprecated features in RabbitMQ 4.x

# Memory and disk settings
vm_memory_high_watermark.relative = 0.6
disk_free_limit.absolute = 2GB

# Connection and heartbeat settings
heartbeat = 60

# Note: management_metrics_collection deprecation warning is expected
# and will be handled automatically by RabbitMQ 4.x in future versions
# No configuration is needed to suppress it

